#include <iostream>
#include "gametime.h"

int main() {
    startBlackjack();
    // Deck currDeck;
    // currDeck.displayDeck();
    // currDeck.resetDeck();
    // currDeck.displayDeck();

    // while (!currDeck.isEmpty()) {
    //     std::cout << currDeck.removeCard() << std::endl;
    //     //currDeck.displayDeck();
    // }

    return 0;
}